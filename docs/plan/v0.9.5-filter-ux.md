# v0.9.5 — Filter UX improvements

## Goal

Polish the filter/sort controls: icon instead of "Filter" text, drop the
"Oldest" sort option, reorder the filter panel, and replace the producer/country
dropdown lists with text inputs backed by `<datalist>` autocomplete.

## Changes

### 1. Filter button: icon instead of text

Replace the "Filter" label with a funnel SVG icon. Keep the active indicator
dot (•) when filters are active.

### 2. Drop "Oldest" sort option

`app/src/lib/data/filter-sort.ts`:
- Remove `'added-oldest'` from the `SortOption` union type
- Remove the `case 'added-oldest'` branch from `sortWines`

`app/src/lib/data/filter-sort.test.ts`:
- Remove the "sorts by addedAt oldest first" test (behaviour no longer exists)

`app/src/routes/+page.svelte`:
- Remove `'added-oldest'` from `sortLabels`
- Remove it from the `{#each}` sort options list

Vintage descending is already the default (`activeSort = 'vintage-desc'`) — no
change needed.

### 3. Reorder filter panel

New order inside the dropdown:
1. **Bottles** — Single bottle / 0 bottles (moved to top)
2. **Producer** — text input with datalist autocomplete
3. **Country** — text input with datalist autocomplete

### 4. Producer & Country: text inputs with autocomplete

Replace the scrollable button-list dropdowns with `<input type="text">` +
`<datalist>` for each field.

**Producer input:**
- `<datalist id="producers-list">` populated from the `producers` derived array
- On `input` event: find the matching producerKey; set `activeProducer` to that
  key or `null` if no match
- Controlled via a local `producerInput` string state variable
- When the active-filter chip × is clicked, clear both `activeProducer` and
  `producerInput`

**Country input:**
- `<datalist id="countries-list">` populated from the `countries` derived array
- On `input` event: if value is a known country set `activeCountry`, else `null`
- Controlled via a local `countryInput` string state variable
- When the active-filter chip × is clicked, clear both `activeCountry` and
  `countryInput`

Both inputs only shown when there are entries to complete against (same as
current behaviour where the section is hidden when empty).

## Acceptance criteria

- [ ] Filter button shows a funnel icon; active dot still appears when filters
  are set
- [ ] Sort menu has three options: Vintage ↓, Vintage ↑, Newest
- [ ] Filter panel order: Bottles → Producer → Country
- [ ] Producer text input autocompletes from existing producer names
  and activates the filter on exact match
- [ ] Country text input autocompletes from existing countries
  and activates the filter on exact match
- [ ] Clearing a chip also clears the corresponding text input
- [ ] `npm run lint`, `npm run format`, `npm run check`, `npm test` all pass
