# v0.9.7 — Filter row reorder + cycling sort

## Goal
Reorder the filter row and replace the sort dropdown with a cycling button.

## Acceptance criteria
- Filter row order: [Filter icon + dropdown] → [bottle images] → [Sort button]
- Sort button shows the current sort label and cycles through options on each tap:
  vintage-desc → vintage-asc → added-newest → vintage-desc
- No sort dropdown; `showSortMenu` state is removed

## Changes
- `app/src/routes/+page.svelte`
  - Remove `showSortMenu` state
  - Replace `setSort()` with `cycleSort()` using a fixed order array
  - Move bottle image buttons before sort button in template
  - Replace sort dropdown block with a single cycling button
- `app/package.json` — bump to 0.9.7
